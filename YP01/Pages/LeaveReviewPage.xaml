<Page x:Class="YP01.Pages.LeaveReviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Оставить отзыв"
      Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Оставить отзыв на книгу" 
                   FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15">
            <StackPanel>
                <TextBlock Text="Выберите книгу" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <ComboBox x:Name="cbb_books" DisplayMemberPath="Dname" 
                          Height="35" FontSize="13" SelectionChanged="cbb_books_SelectionChanged"/>
                <TextBlock Text="После выбора книги откроется страница для отзыва" 
                           FontSize="11" Foreground="Gray" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>